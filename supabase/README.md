# Supabase

El proyecto Supabase ya existe. Esta carpeta no crea, enlaza ni modifica ningún proyecto remoto.

El commit inicial del repositorio solo incluía README.md: no contenía configuración, migraciones ni funciones. Se reservan las carpetas `migrations/` y `functions/` para incorporar el trabajo existente tras identificar su origen y revisar diferencias. No ejecutar `supabase init`, `db push` ni despliegues para completar esta tarea.

No guardar credenciales reales en Git. Preservar cualquier configuración y migraciones que se incorporen posteriormente.
